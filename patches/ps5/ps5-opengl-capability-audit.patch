diff --git a/tests/ps5/verify_gl33_capability_audit.py b/tests/ps5/verify_gl33_capability_audit.py
--- a/tests/ps5/verify_gl33_capability_audit.py
+++ b/tests/ps5/verify_gl33_capability_audit.py
@@ -2197,7 +2197,12 @@ require("struct pipe_resource *depth_stencil" in EGL and
         "PIPE_BIND_DEPTH_STENCIL" in EGL and
         "case EGL_DEPTH_SIZE: *value = 32" in EGL and
         "case EGL_STENCIL_SIZE: *value = 8" in EGL and
-        "PixelFormat(8, 8, 8, 8), 32, 8, 0" in CTS_PLATFORM and
+        "bits(GL_DEPTH, GL_FRAMEBUFFER_ATTACHMENT_DEPTH_SIZE)" in CTS_PLATFORM and
+        "bits(GL_STENCIL, GL_FRAMEBUFFER_ATTACHMENT_STENCIL_SIZE)" in CTS_PLATFORM and
+        "depth != attribute(EGL_DEPTH_SIZE)" in CTS_PLATFORM and
+        "stencil != attribute(EGL_STENCIL_SIZE)" in CTS_PLATFORM and
+        "samples != attribute(EGL_SAMPLES)" in CTS_PLATFORM and
+        "tcu::PixelFormat(red, green, blue, alpha)" in CTS_PLATFORM and
         "EGL_DEPTH_SIZE" in CTS_PLATFORM and
         "EGL_STENCIL_SIZE" in CTS_PLATFORM,
         "native CTS/default EGL surface lost depth-stencil backing")
